import { Layout } from '../components/Layout';

export default function Scan() {
    return (
        <Layout>
            <div className="flex items-center justify-center min-h-[50vh]">
                <h1 className="text-xl font-bold text-muted-foreground">Scanner (Em Breve)</h1>
            </div>
        </Layout>
    );
}
